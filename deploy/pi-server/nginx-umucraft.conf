server {
    listen 80;
    listen [::]:80;
    server_name _;

    root /srv/umucraft/www;

    location /maps/ {
        autoindex on;
        autoindex_exact_size off;
        autoindex_localtime on;
    }

    location / {
        try_files $uri $uri/ =404;
    }

    client_max_body_size 0;
}
